const express=require('express');const pool=require('../config/db');const {authenticate}=require('../middleware/auth');const {recordEvent,httpError}=require('../services/commerceService');const router=express.Router();router.use(authenticate);
router.get('/',async(req,res,next)=>{try{const r=await pool.query(`SELECT w.id,w.product_id,w.created_at,p.name,p.price_paise,p.currency,p.product_type,p.status,p.images FROM wishlist_items w JOIN products p ON p.id=w.product_id WHERE w.user_id=$1 ORDER BY w.created_at DESC`,[req.user.id]);res.json({items:r.rows});}catch(e){next(e);}});
router.get('/:productId/status',async(req,res,next)=>{try{const r=await pool.query('SELECT 1 FROM wishlist_items WHERE user_id=$1 AND product_id=$2',[req.user.id,req.params.productId]);res.json({saved:r.rowCount>0});}catch(e){next(e);}});
router.post('/:productId',async(req,res,next)=>{try{const exists=await pool.query("SELECT id,category_id,status FROM products WHERE id=$1",[req.params.productId]);if(!exists.rowCount||exists.rows[0].status!=='published')throw httpError('Product not found',404);const r=await pool.query('INSERT INTO wishlist_items(user_id,product_id) VALUES($1,$2) ON CONFLICT(user_id,product_id) DO NOTHING RETURNING id,product_id,created_at',[req.user.id,req.params.productId]);await recordEvent({userId:req.user.id,eventType:'product_saved',productId:req.params.productId,categoryId:exists.rows[0].category_id});res.status(r.rowCount?201:200).json(r.rows[0]||{saved:true,product_id:req.params.productId});}catch(e){next(e);}});
router.delete('/:productId',async(req,res,next)=>{try{const r=await pool.query('DELETE FROM wishlist_items WHERE user_id=$1 AND product_id=$2 RETURNING product_id',[req.user.id,req.params.productId]);if(!r.rowCount)return res.status(404).json({error:'Product is not saved'});await recordEvent({userId:req.user.id,eventType:'product_unsaved',productId:req.params.productId});res.status(204).end();}catch(e){next(e);}});module.exports=router;
