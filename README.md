# Origyn Web

Origyn is a general marketplace with technology as its flagship category.

## Development branches
- `publisher-marketplace-v2` — seller/publisher experience
- `store-v2` — customer storefront
- `master` — protected integration baseline

The publisher and store are intentionally developed separately while sharing the same backend/API and database model.